1) This is the command we used to create a react project at first !
npm create vite@latest clerk-react -- --template react
cd clerk-react
npm install

2) Then we asked Q to download the CSS v3 and 
 Tailwind CSS v3 properly configured and working